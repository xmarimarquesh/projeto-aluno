<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/aluno.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <title>Editar Aluno</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand controle" href="/index">CONTROLE</a><a class="navbar-brand" href="/index">de Estudantes</a>
          <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <p class="nav-link"><strong>Cadastrar: </strong></p>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/cadastroAluno">Alunos</a>
            </li>
            <p class="nav-link">|</p>
            <li class="nav-item">
              <a class="nav-link" href="/cadastroSala">Salas</a>
            </li>
          </ul>
        </div>
      </nav>
      
    <div class="edit-box container" style="max-width: none !important;">
        <h2>Editar informações do aluno</h2>
        <form action="/editarAluno/<%= alunos.IDAluno %>" method="post" enctype="multipart/form-data"  style="width: 95%;">
            <div class="profile">
                <img id="img" src="/img/<%= alunos.Foto %>" onclick="uparImagem()" />
                <input type="file" id="fileInput" accept=".png, .jpeg" name="foto" />
            </div>
            <div class="mb-3 user-box">
                <label>Nome</label>
                <input type="text" class="form-control" value="<%= alunos.Nome %>" name="nome">
            </div>
            <div class="mb-3 user-box">
                <label>Idade</label>
                <input type="text" class="form-control" value="<%= alunos.Idade %>" name="idade">
            </div>
            <div class="mb-3 user-box">
                <label>Sexo</label>
                <input type="text" class="form-control" value="<%= alunos.Sexo %>" name="sexo">
            </div>
            <div class="mb-3 user-box">
                <label>Sala</label>
                <select id="sala" class="form-control" name="sala">
                    <% for (let i=0; i<salas.length; i++) { %>
                        <option value='<%= salas[i].IDSala %>' <%=alunos.IDSala==salas[i].IDSala ? 'selected' : '' %>>
                            <%= salas[i].Nome %>
                        </option>
                        <% } %>
                </select>
            </div>
            <input class="btn btn-primary" type="submit" value="Atualizar" />
        </form>
    </div>
    <footer>
        <a class="navbar-brand" href="index.html">Robert</a><a class="navbar-brand controle" href="index.html">Bosch</a><a
          class="navbar-brand" href="index.html">LTDA</a>
        <img src="https://cdn-icons-png.flaticon.com/256/3513/3513164.png" alt="">
      </footer>
      
      <script src="	https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
      <script src="../js/script.js"></script>
</body>

</html>